import styled from 'styled-components';

const Button = styled.button`
  border-radius: 1rem;
  padding: rem;
  font-size: 2rem;
  font-weight: 900;
  padding: 1em 2em;
  margin-bottom: 3rem;
`;

export default Button;